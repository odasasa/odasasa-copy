import { Wrapper } from "@/components/templates/dashboard/main";

export default async function Payments(params: any) {
  return (
    <Wrapper >
      <div className="flex justify-center items-center"> Payments </div>
    </Wrapper>
  );
}
