import { Wrapper } from "@/components/templates/dashboard/main";

export default async function Wallet(params: any) {
  return (
    <Wrapper>
      <div className="flex justify-center items-center"> Wallet</div>
    </Wrapper>
  );
}
